RPC_URL=
ETHERSCAN_API_KEY=

# Used by the operator wallet that pays gas. Set exactly one.
PRIVATE_KEY=
MNEMONIC="test test test test test test test test test test test junk"

# Optional convenience fallback for sale/protocol scripts.
# Prefer passing --safe 0x... in runbooks so the address is visible in the command.
SAFE_ADDRESS=

# Optional: used by scripts/sale.ts and scripts/protocol.ts --action propose-safe.
# Hosted Safe Transaction Service may require an API key.
SAFE_API_KEY=
SAFE_TX_SERVICE_URL=

# Optional sale schedule convenience values. These are Unix seconds.
# Passing --cca-start-timestamp/--cca-end-timestamp in the command is preferred
# for release runs so the schedule is visible in shell history.
BRACKEN_CCA_START=
BRACKEN_CCA_END=
AUCTION_SECONDS_PER_BLOCK=12

# Optional: used by scripts/sale.ts --action prepare to deploy a Predicate
# validation hook. You can also pass --predicate-registry and
# --predicate-policy-id explicitly.
PREDICATE_REGISTRY=
PREDICATE_POLICY_ID=
PREDICATE_ATTESTATION_URL=https://api.predicate.io/v2/attestation
PREDICATE_CHAIN=

# Optional: used by packages/bracken-sale dev proxy/backend for gated bids.
# Do not expose this in browser-visible client env.
PREDICATE_API_KEY=

# Optional: LiquidityLauncher/LBP overrides. Sepolia/mainnet defaults are built
# into scripts/sale.ts, so leave empty unless Uniswap gives a different address.
LIQUIDITY_LAUNCHER=
LBP_STRATEGY=

# Optional protocol deployment address overrides.
# Prefer explicit config files for release, but these are supported by scripts/protocol.
FEE_TOKEN=
PROTOCOL_TREASURY=
SLASHED_FUNDS_TREASURY=
